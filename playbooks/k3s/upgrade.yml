---
# Upgrade all k3s nodes to the version in group_vars/k3s_cluster.yml.
# Servers are upgraded one at a time (serial: 1) to avoid etcd learner issues.
# Agents are upgraded in parallel.
#
# Usage:
#   ansible-playbook playbooks/k3s/upgrade.yml --ask-pass --ask-become-pass

- import_playbook: ../../vendor/k3s-ansible/playbooks/upgrade.yml
